# Configuration file for bootstraping the package.
#
# (c) Thomas Guillod - Dartmouth College

[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools", "wheel", "setuptools_scm"]

[tool.setuptools_scm]
write_to = "pypeec/version.py"
