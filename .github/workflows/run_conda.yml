# Run the "Conda tests" with the following conditions.
#   - dispatch
#   - weekly
#
# The test are run on:
#   - Linux / Ubuntu
#   - Python 3.10
#
# Two different kinds of tests are made:
#   - using the "conda_base.yaml" file
#   - using the "conda_dev.yaml" file
#
# Thomas Guillod - Dartmouth College
# Mozilla Public License Version 2.0

name: Run Conda Tests

on:
  workflow_dispatch:
  schedule:
    - cron: "0 2 * * 1"

jobs:
  default:
    strategy:
      fail-fast: false
      matrix:
        method:
          - name: "conda_base"
            cmd:  |
              mambda env create -f conda_base.yaml
              mamba activate pypeec
          - name: "conda_dev"
            cmd:  |
              mambda env create -f conda_dev.yaml
              mamba activate pypeec
          - name: "conda_forge"
            cmd:  |
              mamba create -n pypeec python=3.10
              mamba activate pypeec
              mamba install conda-forge::pypeec
              mamba remove --force --yes --quiet pypeec
    name: ${{ matrix.method.name }}
    runs-on: "ubuntu-latest"
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Set up Conda
        uses: conda-incubator/setup-miniconda@v3
        with:
          miniforge-version: latest
          auto-activate-base: true
          use-mamba: true
      - name: Install dependencies
        shell: bash -el {0}
        run: ${{ matrix.method.cmd }}
      - name: Run the tests
        shell: bash -el {0}
        run: |
          chmod +x scripts/run_tests.sh
          ./scripts/run_tests.sh
